# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# --- OS / editor ---
.DS_Store
.claude/

# --- Jupyter notebooks (kept local, not shared) ---
*.ipynb
.ipynb_checkpoints/

# --- PDFs: project brief + generated reports (kept local, not shared) ---
*.pdf

# --- Generated CFD runs & solver artifacts (regenerated by the tools) ---
cfd_run*/
*.su2
*.vtu
*.msh
*.geo
*.dat
restart*.dat
history.csv
surface_flow.csv
su2_run.log
*.log
*.cfg

# --- Generated reports / case logs / outputs ---
Report/
reports/
report_case_reference/
*_report.pdf
cfd_report*.pdf
design_cases_log.xlsx
_mcp_render_demo.png

# --- Scratch / experiment files ---
# (single leading underscore + lowercase, e.g. _confirm_fix.py — NOT __init__.py)
_[a-z]*.py
*.bak

# (ParaBlade is a git submodule — see .gitmodules — not tracked file-by-file here)
